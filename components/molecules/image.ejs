<figure class="feed--image">
  <% const { id, urls: srcset, alt_description: alt, user: { username }, width, height } = image %>
	<%- include('../atoms/photo', { id, srcset, alt, width, height, index }) %>
	<%- include('../atoms/description', { username }) %>
	<%- include('../atoms/photo-link', { id }) %>
</figure>