<section class="feed">
	<% images.map(image => { %>
		<% 
      const { id, urls: srcset, alt_description: alt, user: { username } } = image
      const baseClassName = "feed"
    %>
		<%- include('../molecules/image', { id, srcset, alt, username, baseClassName }) %>
	<% }) %>
</section>